<!-- Canonical paste source for Cursor's global User Rules (Cursor Settings > Rules >
User Rules). Cursor stores those rules in its settings database — there is no file
API — so this file cannot be rendered automatically. Keep it as the single source:
edit here, paste into Cursor, then acknowledge with
  node bin/render.mjs --ack-cursor-rules
render.mjs nags whenever this file changes until the new content is acknowledged. -->

Follow the shared agent guardrails in this machine's samebrain repo
(global/guardrails.md). Read the shared memory index at memory/MEMORY.md at
session start if it was not injected automatically.
