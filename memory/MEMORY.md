# Shared Agent Memory — index (one line per fact; details in topics/*.md; cap ~120 lines)

## Accounts & access

## Projects

## Hard rules

## This sync system
- Canonical agent config + shared memory = this repo; render with `node bin/render.mjs`; memory auto-syncs via session hooks (recall at start, push at end)
